[package]
name = "api"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono ={ workspace = true, features = ["serde"]}
jwt = { workspace = true }
palette = { workspace = true }
serde = { workspace = true }
